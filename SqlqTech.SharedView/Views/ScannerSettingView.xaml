<UserControl
    x:Class="SqlqTech.SharedView.Views.ScannerSettingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SqlqTech.SharedView.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="{DynamicResource MaterialDesignCardBackground}"
    Loaded="UserControl_Loaded"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <DockPanel LastChildFill="False">
            <StackPanel
                Margin="10"
                DockPanel.Dock="Right"
                Orientation="Horizontal">
                <Button
                    md:ButtonAssist.CornerRadius="5"
                    Command="{Binding AddCommand}"
                    Content="添加扫描设备" />
                <Button
                    Margin="5,0,5,0"
                    md:ButtonAssist.CornerRadius="5"
                    Command="{Binding SaveCommand}"
                    Content="保存设置" />
            </StackPanel>
        </DockPanel>
        <DataGrid
            Grid.Row="1"
            Margin="10,0,10,10"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserSortColumns="False">
            <DataGrid.Columns>
                <DataGridComboBoxColumn
                    x:Name="cboxScanner"
                    Width="*"
                    Header="型号" />
                <DataGridTextColumn Width="*" Header="端口键" />
                <DataGridTextColumn Width="*" Header="端口名称" />
                <DataGridTextColumn Width="*" Header="端口描述" />
                <DataGridCheckBoxColumn Width="*" Header="启用设备" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
