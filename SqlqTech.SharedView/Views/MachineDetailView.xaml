<UserControl
    x:Class="SqlqTech.SharedView.Views.MachineDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SqlqTech.SharedView.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Margin="10" Content="删除" Command="{Binding DeleteCommand}"/>
            <Button Margin="10" Content="保存" Command="{Binding SaveCommand}"/>
        </StackPanel>
        <DataGrid
            Grid.Row="1"
            Margin="10,0,10,10"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserSortColumns="False"
            IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Header="变量地址" />
                <DataGridTextColumn Width="*" Header="数据类型" />
                <DataGridTextColumn Width="*" Header="描述" />
                <DataGridTextColumn Width="*" Header="是否扫描" />
                <DataGridTextColumn Width="*" Header="扫描周期" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
