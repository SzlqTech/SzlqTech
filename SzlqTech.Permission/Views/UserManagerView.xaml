<UserControl
    x:Class="SzlqTech.Permission.Views.UserManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SzlqTech.Permission.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    d:DesignHeight="800"
    d:DesignWidth="1280"
    Background="{DynamicResource MaterialDesignCardBackground}"
    mc:Ignorable="d">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <StackPanel
                Margin="0,10"
                DockPanel.Dock="Top"
                Orientation="Horizontal">
                <Button Margin="10,0,10,0" Command="{Binding AddCommand}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon
                                Margin="0,0,10,0"
                                VerticalAlignment="Center"
                                Kind="Add" />
                            <TextBlock Text="新建" />
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Command="{Binding SaveCommand}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon
                                Margin="0,0,10,0"
                                VerticalAlignment="Center"
                                Kind="ContentSave" />
                            <TextBlock Text="保存" />
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
            <DataGrid
                Grid.Row="1"
                ItemsSource="{Binding UserVos}"
                Style="{StaticResource baseDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn              
                        Width="*"
                        Binding="{Binding UserName}"
                        Header="名称">
                        <DataGridTextColumn.CellStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.CellStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Code}"
                        Header="代码">
                        <DataGridTextColumn.CellStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.CellStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn  Header="密码" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <PasswordBox                                               
                                    md:PasswordBoxAssist.Password="{Binding DisPlayPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    md:TextFieldAssist.HasClearButton="True"
                                    DockPanel.Dock="Top"
                                    FontSize="14"
                                    Style="{StaticResource MaterialDesignPasswordBox}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridComboBoxColumn
                        x:Name="cbox"
                        Width="*"
                        DisplayMemberPath="Name"
                        Header="角色选择"
                        SelectedItemBinding="{Binding SelectedRoleVo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        SelectedValueBinding="{Binding SelectedRoleCode}"
                        SelectedValuePath="Code">
                        <DataGridComboBoxColumn.CellStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridComboBoxColumn.CellStyle>
                    </DataGridComboBoxColumn>
                    <DataGridTemplateColumn Width="*" Header="操作">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Content="删除" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

    </Grid>
</UserControl>
