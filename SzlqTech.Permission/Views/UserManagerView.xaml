<UserControl
    x:Class="SzlqTech.Permission.Views.UserManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SzlqTech.Permission.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    d:DesignHeight="800"
    d:DesignWidth="1280"
    Background="{DynamicResource MaterialDesignCardBackground}"
    mc:Ignorable="d">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <StackPanel
                Margin="30,10"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Command="{Binding ExcuteCommand}"
                    CommandParameter="Search"
                    Style="{StaticResource darkButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon
                                Margin="0,0,10,0"
                                VerticalAlignment="Center"
                                Kind="Search" />
                            <TextBlock Text="搜索" />
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button
                    Command="{Binding ExcuteCommand}"
                    CommandParameter="Clear"
                    Style="{StaticResource lightButton}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon
                                Margin="0,0,10,0"
                                VerticalAlignment="Center"
                                Kind="Delete" />
                            <TextBlock Text="清空" />
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
            <DockPanel
                Grid.Row="1"
                Margin="10"
                LastChildFill="True">
                <StackPanel
                    Margin="0,10"
                    DockPanel.Dock="Top"
                    Orientation="Horizontal">
                    <Button Command="{Binding AddCommand}" Style="{StaticResource darkButton}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <md:PackIcon
                                    Margin="0,0,10,0"
                                    VerticalAlignment="Center"
                                    Kind="Add" />
                                <TextBlock Text="新建" />
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <Button Command="{Binding DeleteCommand}" Style="{StaticResource darkButton}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <md:PackIcon
                                    Margin="0,0,10,0"
                                    VerticalAlignment="Center"
                                    Kind="ClearBox" />
                                <TextBlock Text="删除" />
                            </StackPanel>
                        </Button.Content>
                    </Button>
                </StackPanel>
                <DataGrid ItemsSource="{Binding UserVos}" Style="{StaticResource baseDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Username}"
                            Header="用户名称">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="FontSize" Value="18" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Code}"
                            Header="用户编码">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="FontSize" Value="18" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Width="*" Header="操作">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <StackPanel Orientation="Horizontal">
                                            <Button
                                                MinWidth="100"
                                                Margin="0,0,5,0"
                                                Command="{Binding DataContext.AssignCommand, RelativeSource={RelativeSource AncestorType=DataGrid, Mode=FindAncestor}}"
                                                CommandParameter="{Binding}"
                                                Content="角色分配" />
                                            <Button
                                                MinWidth="100"
                                                Margin="0,0,5,0"
                                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid, Mode=FindAncestor}}"
                                                CommandParameter="{Binding}"
                                                Content="删除" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Grid>

    </Grid>
</UserControl>
